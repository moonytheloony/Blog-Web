language: go

install: 
    - go get -v github.com/spf13/hugo
    - gem install dpl

script:
    - hugo --theme=hugo_theme_robust -v

branches:
  only:
    - master

after_success:
    - git remote add azure https://rahulrai:TestCredentials@mysimpleapp1.scm.azurewebsites.net:443/mysimpleapp1.git        
    - git push azure master --force --verbose -u