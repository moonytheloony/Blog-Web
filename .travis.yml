language: go

install: 
    - go get -v github.com/spf13/hugo
    - gem install dpl

script:
    - hugo --theme=hugo_theme_robust -v
    - git add public/*

branches:
  only:
    - master

after_success:
    - dpl --provider=AzureWebApps --username=betablog --password=rahulBlog --site=betablogapp1 --verbose